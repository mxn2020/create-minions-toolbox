@import "tailwindcss";

:root {
    --accent: {{accentColor}};
    --accent-hover: {{accentHoverColor}};
    --color-accent: {{accentColor}};
    --color-accent-hover: {{accentHoverColor}};
    --bg: #0F0F23;
    --surface: #1A1A2E;
    --text: #E2E8F0;
    --text-dim: #94A3B8;
}
